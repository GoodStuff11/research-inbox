services:
  research-inbox:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/inbox.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
